# Conversion to YAML from Properties formar report
# Warnings:
# - The yaml file had comments which are lost in the refactoring!
server:
  port: 6541
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ngr?createDatabaseIfNotExist=true
    username: root
    password: Qzdsqzds123@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
    open-in-view: true
    properties:
      hibernate:
        cache:
          use_second_level_cache: false
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  data:
    mongodb:
      uri: mongodb://localhost:27017/NGResearh
      database: NGResearh
